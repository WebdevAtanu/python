nums = [1, 2, 3] # list is an iterable
it = iter(nums) # create an iterator

# iterate through it using next() function to get the next item in the iterator
print(next(it))
print(next(it))
print(next(it))